# Core Dependencies
paddlepaddle==2.6.1
paddleocr>=2.7.0
opencv-python-headless>=4.10.0.84
rapidfuzz>=3.10.0
unidecode>=1.3.8
pandas>=2.2.2
numpy==1.23.5
pydantic>=2.8.2
python-dotenv>=1.0.1
pillow>=10.0.0

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
jinja2>=3.1.2
python-multipart>=0.0.6

# LangChain ecosystem + monitoring
langchain==0.3.26
langchain-community==0.3.26
langchain-pinecone==0.2.8
langchain-text-splitters>=0.3.0
langsmith>=0.2.3
langchain-ollama>=0.1.0

# Pinecone + embeddings
pinecone>=5.0.0
# Keep sentence-transformers compatible with current Torch/Transformers stack
sentence-transformers>=2.2.2,<3.0.0

# Document processing
pypdf>=5.6.1
pymupdf>=1.24.10

# Ollama client
ollama>=0.3.1

# Web search
duckduckgo-search>=5.3.0

# MCP Server
mcp>=1.0.0

# Multimodal processing
# Note: Using openai-clip instead of clip-by-openai to avoid torch version conflicts
# Relax versions to match common CUDA builds on Windows
transformers>=4.24.0
torch>=1.12.0

# Async support
aiofiles>=23.2.0
httpx>=0.25.0

# Utilities
tqdm>=4.66.0
loguru>=0.7.2


# Install CLIP (unquoted for pip compatibility)
git+https://github.com/openai/CLIP.git

# openmeteo-weather
openmeteo-requests
requests-cache
retry-requests